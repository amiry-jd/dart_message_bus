name: message_bus
description: A useful implementation of publish–subscribe messaging pattern using Dart Stream.
version: 0.0.1
homepage: https://github.com/javad-amiry/message_bus
author: javad amiry <javad.d.amiry@gmail.com>

environment:
  sdk: '>=1.20.1 <2.0.0'

dev_dependencies:
  test: ^0.12.0
